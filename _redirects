# Cloudflare Pages / Workers Redirects
# Dynamic route rewrite for Learning Hub subpaths (e.g. /learning-hub/class-10/physics)
# Note: Static files (/notes, /teacher-login, /, etc.) are handled automatically by Cloudflare clean URLs.

/learning-hub/* /learning-hub.html 200
